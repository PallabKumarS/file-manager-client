// enums.ts
export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum PackageType {
  FREE = "FREE",
  SILVER = "SILVER",
  GOLD = "GOLD",
  DIAMOND = "DIAMOND",
}

export enum FileType {
  PDF = "PDF",
  IMAGE = "IMAGE",
  VIDEO = "VIDEO",
  AUDIO = "AUDIO",
}
